<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.angsamo.erp.common.auth.AuthMapper">
	<select id="findActiveByLoginId" resultType="com.angsamo.erp.common.auth.LoginAccount">
		SELECT u.user_id, u.login_id, u.password, u.user_name, u.role,
		       u.department_id, d.department_code, u.vendor_id
		FROM app_user u
		LEFT JOIN department d ON d.department_id = u.department_id
		WHERE u.login_id = #{loginId}
		  AND u.active = 1
	</select>
</mapper>
